{
  "name": "sublet-pipeline-api",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "nest start --watch",
    "build": "prisma generate && nest build",
    "test:cli-build": "npm run build && RUN_CLI_BUILD_SMOKE=1 vitest run test/operations-cli-build.spec.ts",
    "start": "node dist/src/main.js",
    "test": "vitest run",
    "typecheck": "prisma generate && tsc --noEmit",
    "launch:check": "npm run test && npm run typecheck && npm run build && DATABASE_URL=\"${DATABASE_URL:-postgresql://sublet:sublet@localhost:5432/sublet_pipeline?schema=public}\" prisma validate",
    "launch:smoke": "RUN_DB_SMOKE=1 vitest run test/launch-smoke.spec.ts",
    "launch:smoke:roommate": "node -e \"if (!process.env.DATABASE_URL) { console.error('DATABASE_URL is required for launch:smoke:roommate'); process.exit(1) }\" && RUN_DB_SMOKE=1 vitest run test/roommate-realtime-smoke.spec.ts test/roommate-messaging-migration-safety.spec.ts",
    "launch:smoke:valkey": "node -e \"if (!process.env.VALKEY_URL || !process.env.DATABASE_URL) { console.error('VALKEY_URL and DATABASE_URL are required for launch:smoke:valkey'); process.exit(1) }\" && RUN_DB_SMOKE=1 RUN_VALKEY_SMOKE=1 vitest run test/valkey-rate-limit.integration.spec.ts test/roommate-valkey-realtime-smoke.spec.ts",
    "seed:la": "DATABASE_URL=\"${DATABASE_URL:-postgresql://sublet:sublet@localhost:5432/sublet_pipeline?schema=public}\" ts-node -r tsconfig-paths/register src/seed-los-angeles.ts",
    "beta:invites": "node dist/src/operations/beta-invites.cli.js",
    "beta:invites:dev": "ts-node -r tsconfig-paths/register src/operations/beta-invites.cli.ts",
    "admin:roles": "node dist/src/operations/admin-roles.cli.js",
    "admin:roles:dev": "ts-node -r tsconfig-paths/register src/operations/admin-roles.cli.ts",
    "prisma": "prisma"
  },
  "dependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/config": "^3.3.0",
    "@nestjs/core": "^10.4.15",
    "@nestjs/jwt": "^10.2.0",
    "@nestjs/platform-express": "^10.4.15",
    "@nestjs/platform-socket.io": "10.4.15",
    "@nestjs/websockets": "10.4.15",
    "@prisma/client": "^6.1.0",
    "@socket.io/redis-adapter": "^8.3.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "file-type": "20.4.1",
    "redis": "^5.8.2",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "sharp": "0.34.5",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.8",
    "@nestjs/testing": "^10.4.15",
    "@types/node": "^22.10.2",
    "prisma": "^6.1.0",
    "socket.io-client": "4.8.3",
    "supertest": "^7.0.0",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
